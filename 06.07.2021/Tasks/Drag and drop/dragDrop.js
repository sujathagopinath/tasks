/* draggable element */
const dragElem = document.querySelector('.drag-item');

dragElem.addEventListener('dragstart', dragStart);

function dragStart(e) {
    e.dataTransfer.setData('text/plain', e.target.id);
    setTimeout(() => {
        e.target.classList.add('hide');
    }, 0);
}


/* drop targets */
const rectangleBox = document.querySelectorAll('.rectangle');

rectangleBox.forEach(rectangleBox => {
    rectangleBox.addEventListener('dragenter', dragEnter)
    rectangleBox.addEventListener('dragover', dragOver);
    rectangleBox.addEventListener('dragleave', dragLeave);
    rectangleBox.addEventListener('drop', drop);
});


function dragEnter(e) {
    e.preventDefault();
    e.target.classList.add('drag-over');
}

function dragOver(e) {
    e.preventDefault();
    e.target.classList.add('drag-over');
}

function dragLeave(e) {
    e.target.classList.remove('drag-over');
}

function drop(e) {
    e.target.classList.remove('drag-over');

    // get the draggable element
    const id = e.dataTransfer.getData('text/plain');
    const draggable = document.getElementById(id);

    // add it to the drop target
    e.target.appendChild(draggable);

    // display the draggable element
    draggable.classList.remove('hide');
}